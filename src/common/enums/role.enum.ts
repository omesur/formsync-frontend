// src/common/enums/role.enum.ts
export enum Role {
    User = 'User', // Rol básico por defecto
    QuickFiller = 'QuickFiller',
    SyncManager = 'SyncManager',
    QuickBuilder = 'QuickBuilder',
    DocBuilder = 'DocBuilder',
    SyncMaster = 'SyncMaster',
    Admin = 'Admin', // Podríamos necesitar un rol Admin/Superusuario también
  }